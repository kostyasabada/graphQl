import { makeStyles } from 'tss-react/mui';

export const usedifferenceViewerStyles = makeStyles()(() => ({
  diffContainer: {
    backgroundColor: '#efefef',
    padding: '6px',
    border: '2px solid #1976d2',
    borderRadius: '4px',
    width: '70vw',
  },
  
}));